"use client"
import { useRouter } from 'next/navigation'

export default function EditingMediaPage() {
    const router = useRouter();
console.log(router)
    return (
        <div className="">{}</div>
    )
}

//how to get data from links from anotehr page